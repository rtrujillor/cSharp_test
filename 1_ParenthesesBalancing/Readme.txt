Implement the method in ParenthesesBalancing.cs, as indicated in the method summary.

Unit tests are available. They are based on NUnit test framework. 
How to use:
- set the project as Startup Project
- use Ctrl + F5 to run all tests in the project without debugging. The failures are displayed in a console window.
- use F5 to debug the tests in the project.

NOTE: Changing/removing existing unit tests is NOT allowed.
      Adding new unit tests is allowed.